import { OrdenarListaPipe } from './ordenar-lista.pipe';

describe('OrdenarListaPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarListaPipe();
    expect(pipe).toBeTruthy();
  });
});
